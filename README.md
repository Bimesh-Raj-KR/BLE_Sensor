# Socket_Communication

A C project to establish connection between a **server** and a **client** using **socket** and 
facilitate **File transfer**

## Features

**Client** sends a file using command line argument **./client fileName ip**    
**Server** then stores the received files in **Received_Files** directory   

## Building with Make

### $ make client

Output: client

### $ make server

Output: server

